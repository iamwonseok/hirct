// AUTO-GENERATED by hirct-gen — DO NOT EDIT
#include <cstdlib>
#include <cstdint>
#include <iostream>
#include <random>
#include <string>
#include <verilated.h>
#include <verilated_vcd_c.h>
#include "Vtempsensor_test_mux.h"
#include "../cmodel/tempsensor_test_mux.h"

int main(int argc, char **argv) {
  int cycles = 100;
  int seed = 1;
  if (argc > 1) cycles = std::atoi(argv[1]);
  if (argc > 2) seed = std::atoi(argv[2]);
  if (cycles <= 0) cycles = 1;
  std::mt19937_64 rng(seed);
  Verilated::randReset(2);
  Vtempsensor_test_mux rtl;
  tempsensor_test_mux model;
  model.do_reset();
  VerilatedVcdC* vcd = nullptr;
  if (argc > 3 && std::string(argv[3]) == "--dump-vcd") {
    Verilated::traceEverOn(true);
    vcd = new VerilatedVcdC;
    rtl.trace(vcd, 99);
    std::string vcd_file = std::string("debug_") + argv[2] + ".vcd";
    vcd->open(vcd_file.c_str());
  }
  rtl.F_ETS_CLK = 0;
  int pass = 0;
  int fail = 0;
  for (int cyc = 0; cyc < cycles; ++cyc) {
    bool in_dft_tempsensor_mode = static_cast<bool>((rng() & 1));
    rtl.dft_tempsensor_mode = in_dft_tempsensor_mode;
    model.dft_tempsensor_mode = in_dft_tempsensor_mode;
    bool in_F_ETS_RSTn = static_cast<bool>((rng() & 1));
    rtl.F_ETS_RSTn = in_F_ETS_RSTn;
    model.F_ETS_RSTn = in_F_ETS_RSTn;
    bool in_F_ETS_PD = static_cast<bool>((rng() & 1));
    rtl.F_ETS_PD = in_F_ETS_PD;
    model.F_ETS_PD = in_F_ETS_PD;
    bool in_F_ETS_CLOAD = static_cast<bool>((rng() & 1));
    rtl.F_ETS_CLOAD = in_F_ETS_CLOAD;
    model.F_ETS_CLOAD = in_F_ETS_CLOAD;
    uint8_t in_F_ETS_CFG0 = static_cast<uint8_t>((static_cast<uint32_t>(rng()) & ((1u << 8) - 1u)));
    rtl.F_ETS_CFG0 = in_F_ETS_CFG0;
    model.F_ETS_CFG0 = in_F_ETS_CFG0;
    uint8_t in_F_ETS_CFGA = static_cast<uint8_t>((static_cast<uint32_t>(rng()) & ((1u << 8) - 1u)));
    rtl.F_ETS_CFGA = in_F_ETS_CFGA;
    model.F_ETS_CFGA = in_F_ETS_CFGA;
    bool in_F_ETS_RUN = static_cast<bool>((rng() & 1));
    rtl.F_ETS_RUN = in_F_ETS_RUN;
    model.F_ETS_RUN = in_F_ETS_RUN;
    bool in_dft_ets_clk = static_cast<bool>((rng() & 1));
    rtl.dft_ets_clk = in_dft_ets_clk;
    model.dft_ets_clk = in_dft_ets_clk;
    bool in_dft_ets_rstn = static_cast<bool>((rng() & 1));
    rtl.dft_ets_rstn = in_dft_ets_rstn;
    model.dft_ets_rstn = in_dft_ets_rstn;
    bool in_dft_ets_pd = static_cast<bool>((rng() & 1));
    rtl.dft_ets_pd = in_dft_ets_pd;
    model.dft_ets_pd = in_dft_ets_pd;
    bool in_dft_ets_cload = static_cast<bool>((rng() & 1));
    rtl.dft_ets_cload = in_dft_ets_cload;
    model.dft_ets_cload = in_dft_ets_cload;
    uint8_t in_dft_ets_cfg0 = static_cast<uint8_t>((static_cast<uint32_t>(rng()) & ((1u << 8) - 1u)));
    rtl.dft_ets_cfg0 = in_dft_ets_cfg0;
    model.dft_ets_cfg0 = in_dft_ets_cfg0;
    uint8_t in_dft_ets_cfga = static_cast<uint8_t>((static_cast<uint32_t>(rng()) & ((1u << 8) - 1u)));
    rtl.dft_ets_cfga = in_dft_ets_cfga;
    model.dft_ets_cfga = in_dft_ets_cfga;
    bool in_dft_ets_run = static_cast<bool>((rng() & 1));
    rtl.dft_ets_run = in_dft_ets_run;
    model.dft_ets_run = in_dft_ets_run;
    bool in_ETS_RDY = static_cast<bool>((rng() & 1));
    rtl.ETS_RDY = in_ETS_RDY;
    model.ETS_RDY = in_ETS_RDY;
    uint16_t in_ETS_DOUT = static_cast<uint16_t>((static_cast<uint32_t>(rng()) & ((1u << 16) - 1u)));
    rtl.ETS_DOUT = in_ETS_DOUT;
    model.ETS_DOUT = in_ETS_DOUT;
    bool in_ETS_DOUT_TYPE = static_cast<bool>((rng() & 1));
    rtl.ETS_DOUT_TYPE = in_ETS_DOUT_TYPE;
    model.ETS_DOUT_TYPE = in_ETS_DOUT_TYPE;
    bool in_ETS_FAULTn = static_cast<bool>((rng() & 1));
    rtl.ETS_FAULTn = in_ETS_FAULTn;
    model.ETS_FAULTn = in_ETS_FAULTn;
    rtl.F_ETS_CLK = 0;
    rtl.eval();
    if (vcd) vcd->dump(cyc * 2);
    rtl.F_ETS_CLK = 1;
    rtl.eval();
    if (vcd) vcd->dump(cyc * 2 + 1);
    model.step();
    if (static_cast<uint64_t>(rtl.F_ETS_RDY) != static_cast<uint64_t>(model.F_ETS_RDY)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=F_ETS_RDY rtl=" << static_cast<uint64_t>(rtl.F_ETS_RDY) << " model=" << static_cast<uint64_t>(model.F_ETS_RDY) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.F_ETS_DOUT) != static_cast<uint64_t>(model.F_ETS_DOUT)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=F_ETS_DOUT rtl=" << static_cast<uint64_t>(rtl.F_ETS_DOUT) << " model=" << static_cast<uint64_t>(model.F_ETS_DOUT) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.F_ETS_DOUT_TYPE) != static_cast<uint64_t>(model.F_ETS_DOUT_TYPE)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=F_ETS_DOUT_TYPE rtl=" << static_cast<uint64_t>(rtl.F_ETS_DOUT_TYPE) << " model=" << static_cast<uint64_t>(model.F_ETS_DOUT_TYPE) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.F_ETS_FAULTn) != static_cast<uint64_t>(model.F_ETS_FAULTn)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=F_ETS_FAULTn rtl=" << static_cast<uint64_t>(rtl.F_ETS_FAULTn) << " model=" << static_cast<uint64_t>(model.F_ETS_FAULTn) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.dft_ets_rdy) != static_cast<uint64_t>(model.dft_ets_rdy)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=dft_ets_rdy rtl=" << static_cast<uint64_t>(rtl.dft_ets_rdy) << " model=" << static_cast<uint64_t>(model.dft_ets_rdy) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.dft_ets_dout) != static_cast<uint64_t>(model.dft_ets_dout)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=dft_ets_dout rtl=" << static_cast<uint64_t>(rtl.dft_ets_dout) << " model=" << static_cast<uint64_t>(model.dft_ets_dout) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.dft_ets_dout_type) != static_cast<uint64_t>(model.dft_ets_dout_type)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=dft_ets_dout_type rtl=" << static_cast<uint64_t>(rtl.dft_ets_dout_type) << " model=" << static_cast<uint64_t>(model.dft_ets_dout_type) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.dft_ets_faultn) != static_cast<uint64_t>(model.dft_ets_faultn)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=dft_ets_faultn rtl=" << static_cast<uint64_t>(rtl.dft_ets_faultn) << " model=" << static_cast<uint64_t>(model.dft_ets_faultn) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.ETS_CLK) != static_cast<uint64_t>(model.ETS_CLK)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=ETS_CLK rtl=" << static_cast<uint64_t>(rtl.ETS_CLK) << " model=" << static_cast<uint64_t>(model.ETS_CLK) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.ETS_RSTn) != static_cast<uint64_t>(model.ETS_RSTn)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=ETS_RSTn rtl=" << static_cast<uint64_t>(rtl.ETS_RSTn) << " model=" << static_cast<uint64_t>(model.ETS_RSTn) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.ETS_PD) != static_cast<uint64_t>(model.ETS_PD)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=ETS_PD rtl=" << static_cast<uint64_t>(rtl.ETS_PD) << " model=" << static_cast<uint64_t>(model.ETS_PD) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.ETS_CLOAD) != static_cast<uint64_t>(model.ETS_CLOAD)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=ETS_CLOAD rtl=" << static_cast<uint64_t>(rtl.ETS_CLOAD) << " model=" << static_cast<uint64_t>(model.ETS_CLOAD) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.ETS_CFG0) != static_cast<uint64_t>(model.ETS_CFG0)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=ETS_CFG0 rtl=" << static_cast<uint64_t>(rtl.ETS_CFG0) << " model=" << static_cast<uint64_t>(model.ETS_CFG0) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.ETS_CFGA) != static_cast<uint64_t>(model.ETS_CFGA)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=ETS_CFGA rtl=" << static_cast<uint64_t>(rtl.ETS_CFGA) << " model=" << static_cast<uint64_t>(model.ETS_CFGA) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.ETS_RUN) != static_cast<uint64_t>(model.ETS_RUN)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=ETS_RUN rtl=" << static_cast<uint64_t>(rtl.ETS_RUN) << " model=" << static_cast<uint64_t>(model.ETS_RUN) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (fail == 0) ++pass;
    if (fail >= 10) break;
  }
  std::cout << (fail ? "FAIL" : "PASS") << ": " << pass
            << " pass, " << fail << " fail (seed=" << seed
            << ", cycles=" << cycles << ")\n";
  if (vcd) { vcd->close(); delete vcd; }
  return fail == 0 ? 0 : 1;
}
