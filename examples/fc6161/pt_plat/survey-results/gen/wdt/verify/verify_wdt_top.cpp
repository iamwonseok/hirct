// AUTO-GENERATED by hirct-gen — DO NOT EDIT
#include <cstdlib>
#include <cstdint>
#include <iostream>
#include <random>
#include <string>
#include <verilated.h>
#include <verilated_vcd_c.h>
#include "Vwdt_top.h"
#include "../cmodel/wdt_top.h"

int main(int argc, char **argv) {
  int cycles = 100;
  int seed = 1;
  if (argc > 1) cycles = std::atoi(argv[1]);
  if (argc > 2) seed = std::atoi(argv[2]);
  if (cycles <= 0) cycles = 1;
  std::mt19937_64 rng(seed);
  Verilated::randReset(2);
  Vwdt_top rtl;
  wdt_top model;
  model.do_reset();
  VerilatedVcdC* vcd = nullptr;
  if (argc > 3 && std::string(argv[3]) == "--dump-vcd") {
    Verilated::traceEverOn(true);
    vcd = new VerilatedVcdC;
    rtl.trace(vcd, 99);
    std::string vcd_file = std::string("debug_") + argv[2] + ".vcd";
    vcd->open(vcd_file.c_str());
  }
  rtl.APB_CLK = 0;
  int pass = 0;
  int fail = 0;
  for (int cyc = 0; cyc < cycles; ++cyc) {
    bool in_WDT0_PRESETn = static_cast<bool>((rng() & 1));
    rtl.WDT0_PRESETn = in_WDT0_PRESETn;
    model.WDT0_PRESETn = in_WDT0_PRESETn;
    bool in_WDT1_PRESETn = static_cast<bool>((rng() & 1));
    rtl.WDT1_PRESETn = in_WDT1_PRESETn;
    model.WDT1_PRESETn = in_WDT1_PRESETn;
    bool in_WDT2_PRESETn = static_cast<bool>((rng() & 1));
    rtl.WDT2_PRESETn = in_WDT2_PRESETn;
    model.WDT2_PRESETn = in_WDT2_PRESETn;
    bool in_WDT3_PRESETn = static_cast<bool>((rng() & 1));
    rtl.WDT3_PRESETn = in_WDT3_PRESETn;
    model.WDT3_PRESETn = in_WDT3_PRESETn;
    bool in_WDT4_PRESETn = static_cast<bool>((rng() & 1));
    rtl.WDT4_PRESETn = in_WDT4_PRESETn;
    model.WDT4_PRESETn = in_WDT4_PRESETn;
    bool in_APB1_PSEL = static_cast<bool>((rng() & 1));
    rtl.APB1_PSEL = in_APB1_PSEL;
    model.APB1_PSEL = in_APB1_PSEL;
    bool in_APB1_PENABLE = static_cast<bool>((rng() & 1));
    rtl.APB1_PENABLE = in_APB1_PENABLE;
    model.APB1_PENABLE = in_APB1_PENABLE;
    bool in_APB1_PWRITE = static_cast<bool>((rng() & 1));
    rtl.APB1_PWRITE = in_APB1_PWRITE;
    model.APB1_PWRITE = in_APB1_PWRITE;
    uint8_t in_APB1_PADDR = static_cast<uint8_t>((static_cast<uint32_t>(rng()) & ((1u << 8) - 1u)));
    rtl.APB1_PADDR = in_APB1_PADDR;
    model.APB1_PADDR = in_APB1_PADDR;
    uint32_t in_APB1_PWDATA = static_cast<uint32_t>((static_cast<uint32_t>(rng())));
    rtl.APB1_PWDATA = in_APB1_PWDATA;
    model.APB1_PWDATA = in_APB1_PWDATA;
    bool in_APB2_PSEL = static_cast<bool>((rng() & 1));
    rtl.APB2_PSEL = in_APB2_PSEL;
    model.APB2_PSEL = in_APB2_PSEL;
    bool in_APB2_PENABLE = static_cast<bool>((rng() & 1));
    rtl.APB2_PENABLE = in_APB2_PENABLE;
    model.APB2_PENABLE = in_APB2_PENABLE;
    bool in_APB2_PWRITE = static_cast<bool>((rng() & 1));
    rtl.APB2_PWRITE = in_APB2_PWRITE;
    model.APB2_PWRITE = in_APB2_PWRITE;
    uint32_t in_APB2_PADDR = static_cast<uint32_t>((static_cast<uint32_t>(rng()) & ((1u << 24) - 1u)));
    rtl.APB2_PADDR = in_APB2_PADDR;
    model.APB2_PADDR = in_APB2_PADDR;
    uint32_t in_APB2_PWDATA = static_cast<uint32_t>((static_cast<uint32_t>(rng())));
    rtl.APB2_PWDATA = in_APB2_PWDATA;
    model.APB2_PWDATA = in_APB2_PWDATA;
    bool in_SCAN_MODE = static_cast<bool>((rng() & 1));
    rtl.SCAN_MODE = in_SCAN_MODE;
    model.SCAN_MODE = in_SCAN_MODE;
    rtl.APB_CLK = 0;
    rtl.eval();
    if (vcd) vcd->dump(cyc * 2);
    rtl.APB_CLK = 1;
    rtl.eval();
    if (vcd) vcd->dump(cyc * 2 + 1);
    model.step();
    if (static_cast<uint64_t>(rtl.APB1_PRDATA) != static_cast<uint64_t>(model.APB1_PRDATA)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=APB1_PRDATA rtl=" << static_cast<uint64_t>(rtl.APB1_PRDATA) << " model=" << static_cast<uint64_t>(model.APB1_PRDATA) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.APB2_PRDATA) != static_cast<uint64_t>(model.APB2_PRDATA)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=APB2_PRDATA rtl=" << static_cast<uint64_t>(rtl.APB2_PRDATA) << " model=" << static_cast<uint64_t>(model.APB2_PRDATA) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.WDT0_INTR) != static_cast<uint64_t>(model.WDT0_INTR)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=WDT0_INTR rtl=" << static_cast<uint64_t>(rtl.WDT0_INTR) << " model=" << static_cast<uint64_t>(model.WDT0_INTR) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.WDT1_INTR) != static_cast<uint64_t>(model.WDT1_INTR)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=WDT1_INTR rtl=" << static_cast<uint64_t>(rtl.WDT1_INTR) << " model=" << static_cast<uint64_t>(model.WDT1_INTR) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.WDT2_INTR) != static_cast<uint64_t>(model.WDT2_INTR)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=WDT2_INTR rtl=" << static_cast<uint64_t>(rtl.WDT2_INTR) << " model=" << static_cast<uint64_t>(model.WDT2_INTR) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.WDT3_INTR) != static_cast<uint64_t>(model.WDT3_INTR)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=WDT3_INTR rtl=" << static_cast<uint64_t>(rtl.WDT3_INTR) << " model=" << static_cast<uint64_t>(model.WDT3_INTR) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.WDT4_INTR) != static_cast<uint64_t>(model.WDT4_INTR)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=WDT4_INTR rtl=" << static_cast<uint64_t>(rtl.WDT4_INTR) << " model=" << static_cast<uint64_t>(model.WDT4_INTR) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.WDT0_RST_REQ) != static_cast<uint64_t>(model.WDT0_RST_REQ)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=WDT0_RST_REQ rtl=" << static_cast<uint64_t>(rtl.WDT0_RST_REQ) << " model=" << static_cast<uint64_t>(model.WDT0_RST_REQ) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.WDT1_RST_REQ) != static_cast<uint64_t>(model.WDT1_RST_REQ)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=WDT1_RST_REQ rtl=" << static_cast<uint64_t>(rtl.WDT1_RST_REQ) << " model=" << static_cast<uint64_t>(model.WDT1_RST_REQ) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.WDT2_RST_REQ) != static_cast<uint64_t>(model.WDT2_RST_REQ)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=WDT2_RST_REQ rtl=" << static_cast<uint64_t>(rtl.WDT2_RST_REQ) << " model=" << static_cast<uint64_t>(model.WDT2_RST_REQ) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.WDT3_RST_REQ) != static_cast<uint64_t>(model.WDT3_RST_REQ)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=WDT3_RST_REQ rtl=" << static_cast<uint64_t>(rtl.WDT3_RST_REQ) << " model=" << static_cast<uint64_t>(model.WDT3_RST_REQ) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.WDT4_RST_REQ) != static_cast<uint64_t>(model.WDT4_RST_REQ)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=WDT4_RST_REQ rtl=" << static_cast<uint64_t>(rtl.WDT4_RST_REQ) << " model=" << static_cast<uint64_t>(model.WDT4_RST_REQ) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (fail == 0) ++pass;
    if (fail >= 10) break;
  }
  std::cout << (fail ? "FAIL" : "PASS") << ": " << pass
            << " pass, " << fail << " fail (seed=" << seed
            << ", cycles=" << cycles << ")\n";
  if (vcd) { vcd->close(); delete vcd; }
  return fail == 0 ? 0 : 1;
}
