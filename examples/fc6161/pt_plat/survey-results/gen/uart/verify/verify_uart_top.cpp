// AUTO-GENERATED by hirct-gen — DO NOT EDIT
#include <cstdlib>
#include <cstdint>
#include <iostream>
#include <random>
#include <string>
#include <verilated.h>
#include <verilated_vcd_c.h>
#include "Vuart_top.h"
#include "../cmodel/uart_top.h"

int main(int argc, char **argv) {
  int cycles = 100;
  int seed = 1;
  if (argc > 1) cycles = std::atoi(argv[1]);
  if (argc > 2) seed = std::atoi(argv[2]);
  if (cycles <= 0) cycles = 1;
  std::mt19937_64 rng(seed);
  Verilated::randReset(2);
  Vuart_top rtl;
  uart_top model;
  model.do_reset();
  VerilatedVcdC* vcd = nullptr;
  if (argc > 3 && std::string(argv[3]) == "--dump-vcd") {
    Verilated::traceEverOn(true);
    vcd = new VerilatedVcdC;
    rtl.trace(vcd, 99);
    std::string vcd_file = std::string("debug_") + argv[2] + ".vcd";
    vcd->open(vcd_file.c_str());
  }
  rtl.UART_CLK = 0;
  int pass = 0;
  int fail = 0;
  for (int cyc = 0; cyc < cycles; ++cyc) {
    bool in_UART_PCLK = static_cast<bool>((rng() & 1));
    rtl.UART_PCLK = in_UART_PCLK;
    model.UART_PCLK = in_UART_PCLK;
    bool in_UART_PRESETn = static_cast<bool>((rng() & 1));
    rtl.UART_PRESETn = in_UART_PRESETn;
    model.UART_PRESETn = in_UART_PRESETn;
    bool in_UART_RESETn = static_cast<bool>((rng() & 1));
    rtl.UART_RESETn = in_UART_RESETn;
    model.UART_RESETn = in_UART_RESETn;
    bool in_PSEL = static_cast<bool>((rng() & 1));
    rtl.PSEL = in_PSEL;
    model.PSEL = in_PSEL;
    bool in_PENABLE = static_cast<bool>((rng() & 1));
    rtl.PENABLE = in_PENABLE;
    model.PENABLE = in_PENABLE;
    bool in_PWRITE = static_cast<bool>((rng() & 1));
    rtl.PWRITE = in_PWRITE;
    model.PWRITE = in_PWRITE;
    uint32_t in_PADDR = static_cast<uint32_t>((static_cast<uint32_t>(rng()) & ((1u << 24) - 1u)));
    rtl.PADDR = in_PADDR;
    model.PADDR = in_PADDR;
    uint32_t in_PWDATA = static_cast<uint32_t>((static_cast<uint32_t>(rng())));
    rtl.PWDATA = in_PWDATA;
    model.PWDATA = in_PWDATA;
    bool in_UART0_RXD = static_cast<bool>((rng() & 1));
    rtl.UART0_RXD = in_UART0_RXD;
    model.UART0_RXD = in_UART0_RXD;
    bool in_UART1_RXD = static_cast<bool>((rng() & 1));
    rtl.UART1_RXD = in_UART1_RXD;
    model.UART1_RXD = in_UART1_RXD;
    bool in_SCAN_MODE = static_cast<bool>((rng() & 1));
    rtl.SCAN_MODE = in_SCAN_MODE;
    model.SCAN_MODE = in_SCAN_MODE;
    rtl.UART_CLK = 0;
    rtl.eval();
    if (vcd) vcd->dump(cyc * 2);
    rtl.UART_CLK = 1;
    rtl.eval();
    if (vcd) vcd->dump(cyc * 2 + 1);
    model.step();
    if (static_cast<uint64_t>(rtl.PRDATA) != static_cast<uint64_t>(model.PRDATA)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=PRDATA rtl=" << static_cast<uint64_t>(rtl.PRDATA) << " model=" << static_cast<uint64_t>(model.PRDATA) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.UART0_INTR) != static_cast<uint64_t>(model.UART0_INTR)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=UART0_INTR rtl=" << static_cast<uint64_t>(rtl.UART0_INTR) << " model=" << static_cast<uint64_t>(model.UART0_INTR) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.UART1_INTR) != static_cast<uint64_t>(model.UART1_INTR)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=UART1_INTR rtl=" << static_cast<uint64_t>(rtl.UART1_INTR) << " model=" << static_cast<uint64_t>(model.UART1_INTR) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.UART0_TXD) != static_cast<uint64_t>(model.UART0_TXD)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=UART0_TXD rtl=" << static_cast<uint64_t>(rtl.UART0_TXD) << " model=" << static_cast<uint64_t>(model.UART0_TXD) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.UART1_TXD) != static_cast<uint64_t>(model.UART1_TXD)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=UART1_TXD rtl=" << static_cast<uint64_t>(rtl.UART1_TXD) << " model=" << static_cast<uint64_t>(model.UART1_TXD) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (fail == 0) ++pass;
    if (fail >= 10) break;
  }
  std::cout << (fail ? "FAIL" : "PASS") << ": " << pass
            << " pass, " << fail << " fail (seed=" << seed
            << ", cycles=" << cycles << ")\n";
  if (vcd) { vcd->close(); delete vcd; }
  return fail == 0 ? 0 : 1;
}
