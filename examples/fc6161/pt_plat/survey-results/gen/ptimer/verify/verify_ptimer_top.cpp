// AUTO-GENERATED by hirct-gen — DO NOT EDIT
#include <cstdlib>
#include <cstdint>
#include <iostream>
#include <random>
#include <string>
#include <verilated.h>
#include <verilated_vcd_c.h>
#include "Vptimer_top.h"
#include "../cmodel/ptimer_top.h"

int main(int argc, char **argv) {
  int cycles = 100;
  int seed = 1;
  if (argc > 1) cycles = std::atoi(argv[1]);
  if (argc > 2) seed = std::atoi(argv[2]);
  if (cycles <= 0) cycles = 1;
  std::mt19937_64 rng(seed);
  Verilated::randReset(2);
  Vptimer_top rtl;
  ptimer_top model;
  model.do_reset();
  VerilatedVcdC* vcd = nullptr;
  if (argc > 3 && std::string(argv[3]) == "--dump-vcd") {
    Verilated::traceEverOn(true);
    vcd = new VerilatedVcdC;
    rtl.trace(vcd, 99);
    std::string vcd_file = std::string("debug_") + argv[2] + ".vcd";
    vcd->open(vcd_file.c_str());
  }
  rtl.TIMER0_CLK = 0;
  int pass = 0;
  int fail = 0;
  for (int cyc = 0; cyc < cycles; ++cyc) {
    bool in_TIMER_PCLK = static_cast<bool>((rng() & 1));
    rtl.TIMER_PCLK = in_TIMER_PCLK;
    model.TIMER_PCLK = in_TIMER_PCLK;
    bool in_TIMER_PRESETn = static_cast<bool>((rng() & 1));
    rtl.TIMER_PRESETn = in_TIMER_PRESETn;
    model.TIMER_PRESETn = in_TIMER_PRESETn;
    bool in_TIMER1_CLK = static_cast<bool>((rng() & 1));
    rtl.TIMER1_CLK = in_TIMER1_CLK;
    model.TIMER1_CLK = in_TIMER1_CLK;
    bool in_TIMER2_CLK = static_cast<bool>((rng() & 1));
    rtl.TIMER2_CLK = in_TIMER2_CLK;
    model.TIMER2_CLK = in_TIMER2_CLK;
    bool in_TIMER3_CLK = static_cast<bool>((rng() & 1));
    rtl.TIMER3_CLK = in_TIMER3_CLK;
    model.TIMER3_CLK = in_TIMER3_CLK;
    bool in_TIMER4_CLK = static_cast<bool>((rng() & 1));
    rtl.TIMER4_CLK = in_TIMER4_CLK;
    model.TIMER4_CLK = in_TIMER4_CLK;
    bool in_TIMER5_CLK = static_cast<bool>((rng() & 1));
    rtl.TIMER5_CLK = in_TIMER5_CLK;
    model.TIMER5_CLK = in_TIMER5_CLK;
    bool in_TIMER6_CLK = static_cast<bool>((rng() & 1));
    rtl.TIMER6_CLK = in_TIMER6_CLK;
    model.TIMER6_CLK = in_TIMER6_CLK;
    bool in_TIMER7_CLK = static_cast<bool>((rng() & 1));
    rtl.TIMER7_CLK = in_TIMER7_CLK;
    model.TIMER7_CLK = in_TIMER7_CLK;
    bool in_TIMER8_CLK = static_cast<bool>((rng() & 1));
    rtl.TIMER8_CLK = in_TIMER8_CLK;
    model.TIMER8_CLK = in_TIMER8_CLK;
    bool in_TIMER9_CLK = static_cast<bool>((rng() & 1));
    rtl.TIMER9_CLK = in_TIMER9_CLK;
    model.TIMER9_CLK = in_TIMER9_CLK;
    bool in_TIMER_RESETn = static_cast<bool>((rng() & 1));
    rtl.TIMER_RESETn = in_TIMER_RESETn;
    model.TIMER_RESETn = in_TIMER_RESETn;
    bool in_PSEL = static_cast<bool>((rng() & 1));
    rtl.PSEL = in_PSEL;
    model.PSEL = in_PSEL;
    bool in_PENABLE = static_cast<bool>((rng() & 1));
    rtl.PENABLE = in_PENABLE;
    model.PENABLE = in_PENABLE;
    bool in_PWRITE = static_cast<bool>((rng() & 1));
    rtl.PWRITE = in_PWRITE;
    model.PWRITE = in_PWRITE;
    uint32_t in_PADDR = static_cast<uint32_t>((static_cast<uint32_t>(rng()) & ((1u << 22) - 1u)));
    rtl.PADDR = in_PADDR;
    model.PADDR = in_PADDR;
    uint32_t in_PWDATA = static_cast<uint32_t>((static_cast<uint32_t>(rng())));
    rtl.PWDATA = in_PWDATA;
    model.PWDATA = in_PWDATA;
    bool in_TIMER0_nCLR = static_cast<bool>((rng() & 1));
    rtl.TIMER0_nCLR = in_TIMER0_nCLR;
    model.TIMER0_nCLR = in_TIMER0_nCLR;
    bool in_TIMER1_nCLR = static_cast<bool>((rng() & 1));
    rtl.TIMER1_nCLR = in_TIMER1_nCLR;
    model.TIMER1_nCLR = in_TIMER1_nCLR;
    bool in_TIMER2_nCLR = static_cast<bool>((rng() & 1));
    rtl.TIMER2_nCLR = in_TIMER2_nCLR;
    model.TIMER2_nCLR = in_TIMER2_nCLR;
    bool in_TIMER3_nCLR = static_cast<bool>((rng() & 1));
    rtl.TIMER3_nCLR = in_TIMER3_nCLR;
    model.TIMER3_nCLR = in_TIMER3_nCLR;
    bool in_TIMER4_nCLR = static_cast<bool>((rng() & 1));
    rtl.TIMER4_nCLR = in_TIMER4_nCLR;
    model.TIMER4_nCLR = in_TIMER4_nCLR;
    bool in_TIMER5_nCLR = static_cast<bool>((rng() & 1));
    rtl.TIMER5_nCLR = in_TIMER5_nCLR;
    model.TIMER5_nCLR = in_TIMER5_nCLR;
    bool in_TIMER6_nCLR = static_cast<bool>((rng() & 1));
    rtl.TIMER6_nCLR = in_TIMER6_nCLR;
    model.TIMER6_nCLR = in_TIMER6_nCLR;
    bool in_TIMER7_nCLR = static_cast<bool>((rng() & 1));
    rtl.TIMER7_nCLR = in_TIMER7_nCLR;
    model.TIMER7_nCLR = in_TIMER7_nCLR;
    bool in_TIMER8_nCLR = static_cast<bool>((rng() & 1));
    rtl.TIMER8_nCLR = in_TIMER8_nCLR;
    model.TIMER8_nCLR = in_TIMER8_nCLR;
    bool in_TIMER9_nCLR = static_cast<bool>((rng() & 1));
    rtl.TIMER9_nCLR = in_TIMER9_nCLR;
    model.TIMER9_nCLR = in_TIMER9_nCLR;
    uint8_t in_TCAP = static_cast<uint8_t>((static_cast<uint32_t>(rng()) & ((1u << 4) - 1u)));
    rtl.TCAP = in_TCAP;
    model.TCAP = in_TCAP;
    rtl.TIMER0_CLK = 0;
    rtl.eval();
    if (vcd) vcd->dump(cyc * 2);
    rtl.TIMER0_CLK = 1;
    rtl.eval();
    if (vcd) vcd->dump(cyc * 2 + 1);
    model.step();
    if (static_cast<uint64_t>(rtl.PRDATA) != static_cast<uint64_t>(model.PRDATA)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=PRDATA rtl=" << static_cast<uint64_t>(rtl.PRDATA) << " model=" << static_cast<uint64_t>(model.PRDATA) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.TIMER_INTR) != static_cast<uint64_t>(model.TIMER_INTR)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=TIMER_INTR rtl=" << static_cast<uint64_t>(rtl.TIMER_INTR) << " model=" << static_cast<uint64_t>(model.TIMER_INTR) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.TIMER0_CLR_REQ) != static_cast<uint64_t>(model.TIMER0_CLR_REQ)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=TIMER0_CLR_REQ rtl=" << static_cast<uint64_t>(rtl.TIMER0_CLR_REQ) << " model=" << static_cast<uint64_t>(model.TIMER0_CLR_REQ) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.TIMER1_CLR_REQ) != static_cast<uint64_t>(model.TIMER1_CLR_REQ)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=TIMER1_CLR_REQ rtl=" << static_cast<uint64_t>(rtl.TIMER1_CLR_REQ) << " model=" << static_cast<uint64_t>(model.TIMER1_CLR_REQ) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.TIMER2_CLR_REQ) != static_cast<uint64_t>(model.TIMER2_CLR_REQ)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=TIMER2_CLR_REQ rtl=" << static_cast<uint64_t>(rtl.TIMER2_CLR_REQ) << " model=" << static_cast<uint64_t>(model.TIMER2_CLR_REQ) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.TIMER3_CLR_REQ) != static_cast<uint64_t>(model.TIMER3_CLR_REQ)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=TIMER3_CLR_REQ rtl=" << static_cast<uint64_t>(rtl.TIMER3_CLR_REQ) << " model=" << static_cast<uint64_t>(model.TIMER3_CLR_REQ) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.TIMER4_CLR_REQ) != static_cast<uint64_t>(model.TIMER4_CLR_REQ)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=TIMER4_CLR_REQ rtl=" << static_cast<uint64_t>(rtl.TIMER4_CLR_REQ) << " model=" << static_cast<uint64_t>(model.TIMER4_CLR_REQ) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.TIMER5_CLR_REQ) != static_cast<uint64_t>(model.TIMER5_CLR_REQ)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=TIMER5_CLR_REQ rtl=" << static_cast<uint64_t>(rtl.TIMER5_CLR_REQ) << " model=" << static_cast<uint64_t>(model.TIMER5_CLR_REQ) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.TIMER6_CLR_REQ) != static_cast<uint64_t>(model.TIMER6_CLR_REQ)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=TIMER6_CLR_REQ rtl=" << static_cast<uint64_t>(rtl.TIMER6_CLR_REQ) << " model=" << static_cast<uint64_t>(model.TIMER6_CLR_REQ) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.TIMER7_CLR_REQ) != static_cast<uint64_t>(model.TIMER7_CLR_REQ)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=TIMER7_CLR_REQ rtl=" << static_cast<uint64_t>(rtl.TIMER7_CLR_REQ) << " model=" << static_cast<uint64_t>(model.TIMER7_CLR_REQ) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.TIMER8_CLR_REQ) != static_cast<uint64_t>(model.TIMER8_CLR_REQ)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=TIMER8_CLR_REQ rtl=" << static_cast<uint64_t>(rtl.TIMER8_CLR_REQ) << " model=" << static_cast<uint64_t>(model.TIMER8_CLR_REQ) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.TIMER9_CLR_REQ) != static_cast<uint64_t>(model.TIMER9_CLR_REQ)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=TIMER9_CLR_REQ rtl=" << static_cast<uint64_t>(rtl.TIMER9_CLR_REQ) << " model=" << static_cast<uint64_t>(model.TIMER9_CLR_REQ) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.TOUT) != static_cast<uint64_t>(model.TOUT)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=TOUT rtl=" << static_cast<uint64_t>(rtl.TOUT) << " model=" << static_cast<uint64_t>(model.TOUT) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (fail == 0) ++pass;
    if (fail >= 10) break;
  }
  std::cout << (fail ? "FAIL" : "PASS") << ": " << pass
            << " pass, " << fail << " fail (seed=" << seed
            << ", cycles=" << cycles << ")\n";
  if (vcd) { vcd->close(); delete vcd; }
  return fail == 0 ? 0 : 1;
}
