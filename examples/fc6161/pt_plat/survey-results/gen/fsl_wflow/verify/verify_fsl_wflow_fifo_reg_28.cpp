// AUTO-GENERATED by hirct-gen — DO NOT EDIT
#include <cstdlib>
#include <cstdint>
#include <iostream>
#include <random>
#include <string>
#include <verilated.h>
#include <verilated_vcd_c.h>
#include "Vfsl_wflow_fifo_reg_28.h"
#include "../cmodel/fsl_wflow_fifo_reg_28.h"

int main(int argc, char **argv) {
  int cycles = 100;
  int seed = 1;
  if (argc > 1) cycles = std::atoi(argv[1]);
  if (argc > 2) seed = std::atoi(argv[2]);
  if (cycles <= 0) cycles = 1;
  std::mt19937_64 rng(seed);
  Verilated::randReset(2);
  Vfsl_wflow_fifo_reg_28 rtl;
  fsl_wflow_fifo_reg_28 model;
  model.do_reset();
  VerilatedVcdC* vcd = nullptr;
  if (argc > 3 && std::string(argv[3]) == "--dump-vcd") {
    Verilated::traceEverOn(true);
    vcd = new VerilatedVcdC;
    rtl.trace(vcd, 99);
    std::string vcd_file = std::string("debug_") + argv[2] + ".vcd";
    vcd->open(vcd_file.c_str());
  }
  rtl.i_clk = 0;
  int pass = 0;
  int fail = 0;
  for (int cyc = 0; cyc < cycles; ++cyc) {
    bool in_i_rstb = static_cast<bool>((rng() & 1));
    rtl.i_rstb = in_i_rstb;
    model.i_rstb = in_i_rstb;
    uint8_t in_i_arcfg = static_cast<uint8_t>((static_cast<uint32_t>(rng()) & ((1u << 7) - 1u)));
    rtl.i_arcfg = in_i_arcfg;
    model.i_arcfg = in_i_arcfg;
    bool in_i_wr_valid = static_cast<bool>((rng() & 1));
    rtl.i_wr_valid = in_i_wr_valid;
    model.i_wr_valid = in_i_wr_valid;
    uint32_t in_i_wr_data = static_cast<uint32_t>((static_cast<uint32_t>(rng())));
    rtl.i_wr_data = in_i_wr_data;
    model.i_wr_data = in_i_wr_data;
    bool in_i_rd_ready = static_cast<bool>((rng() & 1));
    rtl.i_rd_ready = in_i_rd_ready;
    model.i_rd_ready = in_i_rd_ready;
    rtl.i_clk = 0;
    rtl.eval();
    if (vcd) vcd->dump(cyc * 2);
    rtl.i_clk = 1;
    rtl.eval();
    if (vcd) vcd->dump(cyc * 2 + 1);
    model.step();
    if (static_cast<uint64_t>(rtl.o_ecnt) != static_cast<uint64_t>(model.o_ecnt)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=o_ecnt rtl=" << static_cast<uint64_t>(rtl.o_ecnt) << " model=" << static_cast<uint64_t>(model.o_ecnt) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.o_wr_ready) != static_cast<uint64_t>(model.o_wr_ready)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=o_wr_ready rtl=" << static_cast<uint64_t>(rtl.o_wr_ready) << " model=" << static_cast<uint64_t>(model.o_wr_ready) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.o_wr_aready) != static_cast<uint64_t>(model.o_wr_aready)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=o_wr_aready rtl=" << static_cast<uint64_t>(rtl.o_wr_aready) << " model=" << static_cast<uint64_t>(model.o_wr_aready) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.o_rd_valid) != static_cast<uint64_t>(model.o_rd_valid)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=o_rd_valid rtl=" << static_cast<uint64_t>(rtl.o_rd_valid) << " model=" << static_cast<uint64_t>(model.o_rd_valid) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (static_cast<uint64_t>(rtl.o_rd_data) != static_cast<uint64_t>(model.o_rd_data)) {
      std::cerr << "FAIL cycle=" << cyc << " seed=" << seed
                << " port=o_rd_data rtl=" << static_cast<uint64_t>(rtl.o_rd_data) << " model=" << static_cast<uint64_t>(model.o_rd_data) << "\n";
      ++fail;
      if (fail >= 10) break;
    }
    if (fail == 0) ++pass;
    if (fail >= 10) break;
  }
  std::cout << (fail ? "FAIL" : "PASS") << ": " << pass
            << " pass, " << fail << " fail (seed=" << seed
            << ", cycles=" << cycles << ")\n";
  if (vcd) { vcd->close(); delete vcd; }
  return fail == 0 ? 0 : 1;
}
